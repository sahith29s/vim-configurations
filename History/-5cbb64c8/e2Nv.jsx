import { createSlice } from "@reduxjs/toolkit";

const users = createSlice({
    name: "sahith",
    initialState: [],
    reducers: {
        addUser(state, action) { },
        removeUser(state, action) { },reverse engineering full course reverse engineering full course 
        deleteUsers(state, action) { },
    }
})

export { users }
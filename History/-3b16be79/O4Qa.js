const mongoose = require("mongoose");

const registerSchema = new mongoose.Schema({
    name : {
        type : String,
        requied : true
    }
    name : {
        type : String,
        requied : true
    }
    name : {
        type : String,
        requied : true
    }
    name : {
        type : String,
        requied : true
    }
    
})
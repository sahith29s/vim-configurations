import { createSlice } from "@reduxjs/toolkit";

export const Obj = createSlice({
    name : "obj",
    initialState : {
        value : 10
    },
    reducers : {
        add : state =>{
            
        }
    }


})
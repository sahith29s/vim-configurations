import { useCallback } from "react"


const De = () => {
    
    const handleChange = useCallback((e) =>{

    } , )
    return (
        <>
            <input type="text" onChange={handleChange} />
        </>
    )
}

export default De
import { useCallback } from "react"

const De = () => {

    const handleChange = () =>{
        
    };

    return (
        <>
            <input type="text" onChange={handleChange} />
        </>
    )
}

export default De
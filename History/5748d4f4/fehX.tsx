import { useCallback } from "react"

const De = () => {

    const handleChange = () =>{
        debounce
    };

    return (
        <>
            <input type="text" onChange={handleChange} />
        </>
    )
}

export default De